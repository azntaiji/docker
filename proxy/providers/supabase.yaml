http:
  routers:
    supabase:
      rule: "Host(`supabase.zachtaiji.com`)"
      entryPoints:
        - websecure
      service: supabase
      tls:
        certResolver: myresolver

  services:
    supabase:
      loadBalancer:
        servers:
          - url: "http://kong:8000"